import { ValidationRule } from '../validation';

/**
 * Apply-time rules for the Secondary board. None today — the board runs the
 * same generic path, so adding one later is a config-only change here.
 */
export const SECONDARY_COMMANDS_RULES: ValidationRule[] = [];
